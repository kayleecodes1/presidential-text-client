//import { callApi } from './util.js';

export function create(title, text, date, speakerId, customLabels) {

    return new Promise((resolve) => {
        setTimeout(() => {
            resolve({
                //TODO
            });
        }, 1000);
    });
    //return callApi('documents', 'POST', { title, text, date, speakerId, customLabels });
}

//TODO: update

//TODO: delete
